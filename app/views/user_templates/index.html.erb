<h1>
  All users
</h1>

<a href="/users/new">
  Add a new user
</a>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<table class="table">
  <tr>
    <th>
      ID
    </th>

    <th>
      Email
    </th>

    <th>
      Password
    </th>

    <th>
      Created at
    </th>

    <th>
      Updated at
    </th>

    <th>
    </th>
  </tr>

  <% @users.each do |user| %>
  <tr>
    <!-- Display user.id start -->
    <td>
      <%= user.id %>
    </td>
    <!-- Display user.id end -->

    <!-- Display user.email start -->
    <td>
      <%= user.email %>
    </td>
    <!-- Display user.email end -->

    <!-- Display user.password start -->
    <td>
      <%= user.password %>
    </td>
    <!-- Display user.password end -->

    <!-- Display user.created_at start -->
    <td>
      <%= time_ago_in_words(user.created_at) %> ago
    </td>
    <!-- Display user.created_at end -->

    <!-- Display user.updated_at start -->
    <td>
      <%= time_ago_in_words(user.updated_at) %> ago
    </td>
    <!-- Display user.updated_at end -->

    <td>
      <a href="/users/<%= user.id %>">
        Show details
      </a>
    </td>
  </tr>
  <% end %>
</table>
    <%= paginate @users, theme: 'twitter-bootstrap-4' %>
